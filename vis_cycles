#!/usr/bin/env python3
"""Local wrapper for the vis_cycles CLI without package installation."""

import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent
sys.path.insert(0, str(ROOT / "src"))

from hormone_cycler.visualization import main


if __name__ == "__main__":
    raise SystemExit(main())
